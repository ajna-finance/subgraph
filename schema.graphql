type ERC20PoolFactory @entity {
  # factory address
  id: ID!
  # amount of pools created
  poolCount: BigInt!
}

type ERC20Pool @entity {
  # pool address
  id: ID!
  # creation
  createdAtTimestamp: BigInt!
  # block pool was created at
  createdAtBlockNumber: BigInt!
}